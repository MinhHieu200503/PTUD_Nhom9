B1. Nếu không xài hoặc chỉnh sửa file của người khác thì cứ code của mình rồi push lên nhánh của mình
    - Nếu xài code của người khác hoặc chỉnh sửa thì:
        + pull hết code ở main trên git về main trong máy. Chú ý là log ra để xem có đúng là version mới nhất của main trên git hay chưa 
        + NHỚ LÀM TRƯỚC KHI CODE TIẾP: Git rebase main nếu log ra mà thấy mấy commit trước bị trùng thì tốt nhất là "git reset <code commit ở git log> --hard"  về cái commit mới nhất theo như commit của main trên git. Nếu không thì kệ nó, xấu cái commit trên main chứ không ảnh hưởng code
        + Pull rồi thì đổi ra nhánh mình xài git rebase main, Check log để xem có commit version mới nhất như trên main hay chưa. 
        + Nếu pull rebase từ main thành công thì code như bth
B2. Code xog tại 1 cái pull rebase
  - Nếu khoog đúng code của người khác thì sẽ không có conflic
  - Nếu đụng code người khác thì sẽ conflic => xóa cái cũ để xài cái mới. Nếu có lỗi gì thì nhắn tui reset lại cái main

NOTE: ĐỌC ĐỂ TRÁNH TRẢ GIÁ VÌ SAI LẦM
- Khi đổi nhánh NHỚ COMMIT lại code của mình trên nhánh của mình, Thường thì git sẽ nhắc nhưng nếu ở tab khác thì hên xui nó méo nhắc. NHỚ COMMIT NẾU KHÔNG MẤT SẠCH CODE ĐÁY NHÁ
- Quý zị không pull lên main 
- Đặt biệt không xài push --force
